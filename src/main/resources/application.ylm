spring:
  application:
    name: backend_v1
  jackson:
    serialization:
      indent_output: true
  datasource:
    driver-class-name: org.h2.Driver   # driver por defecto si no se sobreescribe
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false